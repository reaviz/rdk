import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { ConnectedOverlay, GlobalOverlay, OverlayTrigger, OverlayPortal } from '../../src';

<Meta title="Docs|Components/Overlay" />

# Overlay

---

Overlay provides component developers a way to position elements globally
or relative to a anchor element. The overlay wraps the backdrop, position, exit
listener and portal to provide a simple API for handling position.

There are five overlay components in the Overlay:

- `ConnectedOverlay` - A overlay that is relative to a anchoring element.
- `GlobalOverlay` - A overlay that is placed on top of other elements globally.
- `OverlayPortal` - A wrapper around the portal component to manage z-indexs.
- `OverlayTrigger` - The internal trigger component that the `ConnectedOverlay` uses.

## Demo

### Global Overlay
A overlay component typically used by components like Dialogs or Drawers.
You can use the Global Ovleray using hooks or element APIs.

#### Element
<Preview withSource="open">
  <Story id="utilities-overlay-global--click-to-open" />
</Preview>

#### Hooks
<Preview withSource="open">
  <Story id="utilities-overlay-global--hooks" />
</Preview>

#### Dialog Example
Below is an example of a simple dialog component using the global overlay.

<Preview withSource="open">
  <Story id="utilities-overlay-global--dialog-example" />
</Preview>

### Connected Overlay
A overlay component typically used by components like menus.

<Preview withSource="open">
  <Story id="utilities-overlay-connected--simple" />
</Preview>

#### Tooltip Example
Below is an example of simple tooltip component using the connected overlay.

<Preview withSource="open">
  <Story id="utilities-overlay-connected--tooltip-example" />
</Preview>

#### Menu Example
Below is an example of simple menu component using the connected overlay.

<Preview withSource="open">
  <Story id="utilities-overlay-connected--menu-example" />
</Preview>

## API
### [src/Overlay/GlobalOverlay.tsx](https://github.com/reaviz/rdk/blob/master/src/Overlay/GlobalOverlay.tsx)
<Props of={GlobalOverlay} />

### [src/Overlay/ConnectedOverlay.tsx](https://github.com/reaviz/rdk/blob/master/src/Overlay/ConnectedOverlay.tsx)
<Props of={ConnectedOverlay} />

### [src/Overlay/OverlayTrigger.tsx](https://github.com/reaviz/rdk/blob/master/src/Overlay/OverlayTrigger.tsx)
<Props of={OverlayTrigger} />

### [src/Overlay/OverlayPortal.tsx](https://github.com/reaviz/rdk/blob/master/src/Overlay/OverlayPortal.tsx)
<Props of={OverlayPortal} />
